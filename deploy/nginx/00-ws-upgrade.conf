# http 块全局 map: WebSocket 升级头映射 (1Panel openresty 默认未带, 多站点反代 WS 都用)
#
# 部署路径: /opt/1panel/www/conf.d/00-ws-upgrade.conf
# 命名前缀 00 保证 nginx 加载顺序最早, 后续 server { } 引用 $connection_upgrade 不报未定义.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ""      close;
}
